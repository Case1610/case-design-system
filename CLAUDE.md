# CLAUDE.md

自分が作るもの全般（見せるサイト／使うアプリ）に使うデザインシステムを、一人で構築するプロジェクト。
目的は学習・ポートフォリオ。

対象領域は 2026-09-11 に業務・管理画面系から拡張した。
原則は目的のレベルで書き、領域による違いは各原則の「領域ごとの現れ方」で扱う。
**新しい領域に進出したときに追加すべきは「現れ方」であって、原則ではない。**

## 最初に読むもの

- **AGENTS.md** — エージェント共通の作業ルール（1セッション1 Issue、終わる前の書き戻し）
- **docs/status.md** — 現在のフェーズと、次にやること
- **docs/principles.md** — 設計原則。すべての判断はここから導出する。ここに反する提案はしない
- **docs/decisions.md** — 原則からの例外と、その根拠（判断ログ）
- **docs/tokens.md** — トークンの階層と命名。値を足すときはここに従う

会話の前提をユーザーに聞き直す前に、まずこの3つを読むこと。

## この場での進め方

- **壁打ち形式**で進める。抽象的な合意ではなく、具体的なトレードオフのシナリオでユーザーに選ばせ、
  そこから原則・仕様を逆算する（"Tailor to the Case" — docs/principles.md 参照）
- 新しい判断が出たら、会話内に留めず、その場で docs/ 配下のファイルに反映する
- 原則に反する提案をする場合は、docs/principles.md の
  「原則同士が衝突したときの裁き方」（観察・仮説・比較・記録）の手順を踏み、
  docs/decisions.md に記録する
- 一度に多くの論点を出さない。1トピックずつ、具体例に絞って進める
- 続きから始めるときは `/design-walkthrough` スキルを使うと、この進め方を踏襲して再開できる

## 技術スタック

- フレームワーク: **React + React Aria**（原則4の a11y 強制内蔵を、自前実装せず満たすため）
- スタイリング: **vanilla-extract**（原則3「逸脱を可視にする」を `createTheme` による型レベルの
  強制として実装するため。CSS変数ベースなので密度モードやモーション分岐の実行時切替とも両立する）
- 言語: TypeScript

この選定は業務・管理画面を前提に決めたもので、対象領域の拡張を受けて再検討の余地がある
（docs/status.md「保留中の論点」参照）。ただし外す判断はしていない。

## 現在のフェーズ

docs/status.md を参照。
