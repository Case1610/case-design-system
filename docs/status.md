# 進捗状況

最終更新: 2026-09-12

## フェーズ

- [x] 設計原則の策定 — docs/principles.md
- [x] 対象領域の拡張と原則の再構成（2026-09-11）— docs/decisions.md
- [ ] Design Tokens 設計 — 階層構造、命名規則、値
- [ ] 最初のコンポーネント実装
- [x] Web 上での公開（Cloudflare Workers、トークン確認ページ）— 2026-09-12
- [ ] ドキュメントサイト（形式は未定。下記「Web 上での公開方法」参照）
- [ ] Figma 連携
- [ ] 技術スタックの再検討（下記「保留中の論点」参照）

## 直近の状態

**2026-09-11: 対象領域を業務・管理画面系から「自分が作るもの全般」へ拡張した。**

ポートフォリオサイトへの適用を検討したところ、旧原則2（色は意味に予約する）が
「ブランド表現の華やかさを捨てる」と書いており、主役が作り手自身である領域と衝突した。
原因は原則の記述に**目的と手段が混在していた**ことで、原則そのものではなかった。

5つの原則を目的レベルの記述へ書き換え、手段は各原則の「領域ごとの現れ方」へ分離した。
原則の数は5のまま。判断の経緯は docs/decisions.md を参照。

| 原則 | 目的（領域不変） |
|---|---|
| 1 | 快適さを我々が決め打ちしない |
| 2 | 意味を伝える資源を、意味以外で消費しない |
| 3 | 逸脱は、禁止するのではなく可視にする |
| 4 | 入力手段を選ばない |
| 5 | 根拠なき逸脱を許さない |

拠り所となる4つの考え方（Form Follows Function / Divide and Conquer /
Begin with the End in Mind / Tailor to the Case）と、原則衝突時の裁定プロトコルは
領域拡張の影響を受けず、そのまま維持している。

## 次のアクション

**Design Tokens を設計する。** 原則2に組み込んだ具体策（生成元の分離、トーン固定・色相解放）が
そのままトークン設計の要件になっている。

決めるべきことの順序:

1. ~~**色空間の選択**~~ → **OKLCH に決定**（2026-09-11、docs/decisions.md）。
   明度は役割に予約し、彩度・色相は解放する
2. ~~**明度の段階**~~ → **6段階に決定**（2026-09-11、docs/decisions.md）。
   `bg` / `surface` / `border` / `text-muted` / `text` / `text-strong`。
   値ではなく役割名で定義し、ライト・ダークで名前は共通、値だけ差し替える
3. ~~**ブランド色スロットの数**~~ → **1色（色相1つ）に決定**（2026-09-11、docs/decisions.md）。
   色相を変数として外出しし、作るものごと／閲覧者ごとに差し替え可能にする。
   これは原則1の軸がひとつ増えたことを意味する（principles.md の原則1「現れ方」に追記済み）
4. ~~**セマンティック色の生成元**~~ → **完了**（2026-09-12）。C を採用し StatusMessage で部品化
   （2026-09-12、docs/decisions.md）。`danger` / `warning` / `success` / `info` を
   1状態につき濃淡2つ＋`onStatus` で contract に入れ、色相は状態ごとの CSS 変数
   （`--ds-hue-danger` ほか）に分離した。明度はライト 45%/94%、ダーク 72%/27% に予約し、
   全360色相でコントラスト比 4.5:1 以上を確認済み。
   **2026-09-12、解き方は C（色だけに頼らない）に決定。** 意味はアイコンと状態名が運び、
   色は補助に回る。色相は慣習どおりに固定（ずらすと「アイコンはエラーなのに色は緑」が起きるため）。
   方針で終わらせず `src/components/StatusMessage.tsx` として部品化し、色・アイコン・状態名を
   別々に使えないようにした
5. **トークンの階層と命名** — Primitive → Semantic → Component
6. **流体スケール** — 型と余白。ビューポート2極を決めて `clamp()` で補間する方式を検討中

## 保留中の論点

### ~~状態色の色相衝突 — A / B / C のどれを採るか~~ → C に決定（2026-09-12）

**公開済みのショーケースで実際に切り替えて比較できる。** 詳細と比較表は docs/decisions.md
（2026-09-12「状態色のトークンを置く」）を参照。

| 案 | 何が起きるか |
|---|---|
| A. 色相を固定 | 状態色は動かない。ブランド色相を27度付近にすると、エラーと見分けがつかない |
| B. 衝突時にずらす | ブランドとは必ず見分けられる。代わりに**エラーが赤でなくなる** |
| C. 色以外の手がかり | 色が一致していてもアイコンと状態名で読める。A・B と併用できる |

決めるのは次の3つ。

1. A か B か（あるいは折衷）
2. C をコンポーネントAPIで必須にするか
3. B を採るなら、確保する角度（暫定35度）

切替の実装は `src/showcase/statusHue.ts` にあり、`src/tokens/` には入れていない。
選ばれたものだけを `tokens/` へ移す。

### 技術スタックの再検討

CLAUDE.md の技術選定（React + React Aria / vanilla-extract）は、
**業務・管理画面を前提に決めたもの**である。

- React Aria は「振る舞いを持つ部品」（Table, Combobox, Modal）のキーボード操作・ARIA・
  フォーカス管理を解決する。原則4を自前実装せず満たすための選択だった
- 一方、見せるサイト側の再利用単位は**セクション**（Hero, Feature, Gallery）であり、
  レイアウトと縦のリズムが主になる。React Aria の出番は相対的に減る
- vanilla-extract は CSS 変数ベースで、密度モードにもモーション分岐にも対応できるため、
  領域拡張の影響を受けにくい

React Aria を外す判断はしていない。原則4（入力手段を選ばない）は領域を問わず
最優先であり、a11y の土台を自前実装に戻す理由はない。
**ただし「主役コンポーネントが変わる」という前提の変化は記録しておく。**

### Web 上での公開方法

**公開済み（2026-09-12）: https://design-system.1610-case.workers.dev**

master への push を GitHub Actions が拾い、`wrangler deploy` で
Cloudflare Workers（Worker 名 `design-system`）へ配信する。case-website と同じ仕組み。
ポートフォリオの Works からもリンクしている（case-website の `src/data/works.ts`）。

いま出しているのは `src/showcase/Showcase.tsx` の1ページで、中身はトークンの確認用。
色相スライダーとダーク／ライト切替があり、**ページ上で原則1（快適さを我々が決め打ちしない）を
実際に動かして確かめられる**ことを狙っている。読ませる説明ではなく、触らせる実物を出す方針。

2026-09-12 に「状態色」節を足した。色相のプリセット（「エラーと同じ (27)」等）で
**原則1と原則2の衝突をひと押しで再現でき**、解き方の候補 A / B / C を切り替えて
見比べられる。ここは結論を見せる場所ではなく、**本人が選ぶための材料を見せる場所**として作ってある。

Storybook は採っていない。フェーズ一覧の「ドキュメントサイト（Storybook）」という記述は
この判断より前のもので、いま決め打ちする理由はない。
コンポーネントが増えて「各部品を独立に触りたい」という必要が出てから、
Storybook を足すか自前のショーケースを育てるかを判断する。

まだ決まっていないこと:
- 設計判断の経緯（principles / decisions）を Web 側に出すかどうか。
  いまはリポジトリの Markdown が唯一の置き場で、サイトからは辿れない
- コンポーネント実装が始まったときの、ショーケースのページ構成

### case-website への適用方針

ポートフォリオサイト（case-website）は現在 Material-UI で構築されている。
このデザインシステムを適用するには MUI を剥がす必要があり、規模が大きい。

case-website 側の docs/status.md に3案（全面移行 / ショーケース1ページ / 先にDSを進める）が
記録されている。未決。
