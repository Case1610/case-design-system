# CLAUDE.md

業務・管理画面向けデザインシステムを一人で構築するプロジェクト。目的は学習・ポートフォリオ。

## 最初に読むもの

- **docs/status.md** — 現在のフェーズと、次にやること
- **docs/principles.md** — 設計原則。すべての判断はここから導出する。ここに反する提案はしない
- **docs/decisions.md** — 原則からの例外と、その根拠（判断ログ）

会話の前提をユーザーに聞き直す前に、まずこの3つを読むこと。

## この場での進め方

- **壁打ち形式**で進める。抽象的な合意ではなく、具体的なトレードオフのシナリオでユーザーに選ばせ、
  そこから原則・仕様を逆算する（"Tailor to the Case" — docs/principles.md 参照）
- 新しい判断が出たら、会話内に留めず、その場で docs/ 配下のファイルに反映する
- 原則に反する提案をする場合は、docs/principles.md の
  「原則同士が衝突したときの裁き方」（観察・仮説・比較・記録）の手順を踏み、
  docs/decisions.md に記録する
- 一度に多くの論点を出さない。1トピックずつ、具体例に絞って進める
- 続きから始めるときは `/design-walkthrough` スキルを使うと、この進め方を踏襲して再開できる

## 技術スタック

- フレームワーク: **React + React Aria**（原則3・4のa11y強制内蔵を、自前実装せず満たすため）
- スタイリング: **vanilla-extract**（原則3「既定で縛る」を `createTheme` による型レベルの
  強制として実装するため。CSS変数ベースなので密度モード等の実行時切替とも両立する）
- 言語: TypeScript

## 現在のフェーズ

docs/status.md を参照。
